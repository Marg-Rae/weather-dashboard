# Weather Dashboard Environment Variables (Example)

# OpenWeatherMap API Configuration
# Get your free API key from: https://openweathermap.org/api
NEXT_PUBLIC_OPENWEATHER_API_KEY=your_openweather_api_key_here

# Optional: Enable real API calls (set to 'true' when you have a valid API key)
NEXT_PUBLIC_USE_REAL_API=false

# Optional: Default city for initial load
NEXT_PUBLIC_DEFAULT_CITY=Mombasa